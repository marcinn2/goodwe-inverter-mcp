apiVersion: v1
kind: Secret
metadata:
  name: goodwe-mcp
type: Opaque
stringData:
  GOODWE_HOST: "192.168.1.100"   # replace with your inverter IP
  MCP_AUTH_TOKEN: ""             # set a strong random token to require Bearer auth on HTTP endpoints
